# Override file for local development
version: '3.8'

services:
  app:
    environment:
      # Override for local development
      NODE_ENV: development
    command: ["npm", "run", "dev"]
    volumes:
      - .:/app
      - /app/node_modules
      - ./uploads:/app/uploads